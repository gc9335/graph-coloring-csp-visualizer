# 25 色实例多解效率分析

该模块在 `le450_25a.col` 上请求多个可行解，观察目标解个数增长时的时间与搜索规模变化。

## 目标解个数 = 1
- 最快算法：`B1`，耗时 0.05s。
- 搜索节点最少算法：`B2`，节点扩展数 450。

## 目标解个数 = 10
- 最快算法：`B1`，耗时 0.07s。
- 搜索节点最少算法：`B2`，节点扩展数 450。

## 目标解个数 = 100
- 最快算法：`B1`，耗时 0.07s。
- 搜索节点最少算法：`B2`，节点扩展数 454。

## 目标解个数 = 1000
- 最快算法：`B1`，耗时 0.09s。
- 搜索节点最少算法：`B4`，节点扩展数 492。

## 1000 解横向比较
- 在 `1000` 个解的目标下，整体最优时间表现来自 `B1`，耗时 0.09s，找到 1000 个解。