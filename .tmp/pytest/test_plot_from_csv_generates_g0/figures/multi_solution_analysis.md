# 25 色实例多解效率分析

该模块在 `le450_25a.col` 上请求多个可行解，观察目标解个数增长时的时间与搜索规模变化。

## 目标解个数 = 1
- 最快算法：`B0`，耗时 0.10s。
- 搜索节点最少算法：`B5`，节点扩展数 40。

## 目标解个数 = 1000
- 最快算法：`B5`，耗时 3.50s。
- 搜索节点最少算法：`B5`，节点扩展数 450。

## 1000 解横向比较
- 在 `1000` 个解的目标下，整体最优时间表现来自 `B5`，耗时 3.50s，找到 1000 个解。